import FileManager from '@/components/FileManager';

export default function FileManagerPage() {
  return (
    <div className="container mx-auto p-6" style={{ height: 'calc(100vh - 100px)' }}>
      <FileManager />
    </div>
  );
}
