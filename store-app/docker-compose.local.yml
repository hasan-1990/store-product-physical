services:
  mongodb:
    image: mongo:7.0
    container_name: store-mongodb-local
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_local_data:/data/db

volumes:
  mongodb_local_data:
