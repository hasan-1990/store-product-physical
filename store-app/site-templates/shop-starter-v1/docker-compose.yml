services:
  mongodb:
    image: mongo:7.0
    container_name: shop-starter-v1-mongo
    restart: unless-stopped
    ports:
      - '127.0.0.1:27017:27017'
    volumes:
      - shop_starter_mongo_data:/data/db

volumes:
  shop_starter_mongo_data:
